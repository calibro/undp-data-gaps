name: Deploy
on:
  push:
    branches:
      - main
jobs:
  build_vue:
    runs-on: ubuntu-latest
    name: Build Vue
    steps:
      - uses: actions/checkout@v2
      - id: Build-Vue
        uses: ashkantaravati/NuxtToGithubPages@v1.0
        with:
          username: 'calibro'
          reponame: 'undp-data-gaps'
          token: ${{ secrets.GITHUB_TOKEN }}
